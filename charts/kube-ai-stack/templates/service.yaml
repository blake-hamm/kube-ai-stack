{{- range .Values.models }}
{{- if .enabled }}
---
{{- $merged := mergeOverwrite (deepCopy $.Values.global) . -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ .name }}
  namespace: {{ $merged.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: {{ $.Values.integrations.argocd.syncWave.service | quote }}
  labels:
    {{- include "kube-ai-stack.modelLabels" (dict "model" . "root" $) | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "kube-ai-stack.modelSelectorLabels" (dict "model" . "root" $) | nindent 4 }}
  ports:
    - name: http
      port: {{ $merged.service.port }}
      targetPort: http
{{- end }}
{{- end }}